#include <iostream>
#include "queque.h"
int main()
{
    int a;
    Queue queue;
    scanf("%d",&a);
    if (!queue.is_full())
    {
        queue.append(a);
    }
    if (!queue.is_empty())
    {

        a = queue.pop();
        printf("%d\n",a);
    }
    return 0;
}



#include "queque.h"

int Queue::pop()
{
    int n = data[head];
    head = (head+1) % 100;
    return n;
}
bool Queue::is_full()
{
    return ((tail+1)%100 == head);
}
bool Queue::is_empty()
{
    return head == tail;
}
void Queue::append(int item)
{
    data[tail] = item;
    tail = (tail+1)% 100;
}



#ifndef queque_h
#define queque_h
#include <cstdio>
class Queue
{
public:
    void append(int item);
    int pop();
    bool is_empty();
    bool is_full();

private:
    int data[100];
    int tail = 0;
    int head = 0;
};
#endif
