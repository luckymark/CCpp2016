#include <stdio.h>

#define LENGTH 100001

struct abc {
    int a;
};

int main(int argc, char const *argv[]) {
    char pi[LENGTH], a[LENGTH], b[LENGTH], t[LENGTH], p[LENGTH];
    char *nums[5] = {pi, a, b, t, p};
    for (size_t i = 0; i < 5; i++) {

    }
    // first term is length,
    printf("%lu\n", sizeof(char));
    return 0;
}
