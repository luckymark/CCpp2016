#include<iostream>
#include<cstdio>
#include<cmath>
using namespace std;
bool prime(int x)
{
	for(int i=2;i<=sqrt(x);i++)
	 if(x%i==0) return 0;
	return 1;
}
int main()
{
	int a;
	scanf("%d",&a);
	if(prime(a)) printf("Yes");
	 else printf("no");
	return 0;
}